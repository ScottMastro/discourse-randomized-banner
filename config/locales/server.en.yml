en:
  site_settings:
    enable_randomized_banner: "Turn on the randomized banner plugin"
    optimize_lcp: "Preload guest_banner and override_banner to improve Largest Contentful Paint (LCP)"
    banner_aspect_ratio: "Predefine the aspect ratio (width/height) to avoid Content Layout Shifts (CLS)"
    override_banner: "If not empty, will show only this single banner for all users including guests"
    guest_banner: "Only show this banner to users not logged in, leave empty to show guests banners from banner_images"
    banner_images: "Banner will be randomly chosen from this list of URLs"
